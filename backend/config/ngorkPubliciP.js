// var ngrok = require("@ngrok/ngrok");

// async function connectToNgorkForPublicIp() {
//     // const connection = await ngork.connect({ authtoken: process.env.NGORK_AUTH_TOKEN })
//     console.log('test')
//     await ngork.authtoken(process.env.NGORK_AUTH_TOKEN);
//     const url = await ngrok.connect(9090);
//     console.log(url)
// }

// module.exports = connectToNgorkForPublicIp